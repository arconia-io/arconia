= RabbitMQ Dev Service
:service-name: rabbitmq

A service providing a https://www.rabbitmq.com[RabbitMQ] event broker for development and testing purposes.

It works with Spring Boot libraries that support AMQP or RabbitMQ, including:

* https://spring.io/projects/spring-amqp[Spring AMQP]
* https://spring.io/projects/spring-cloud-stream[Spring Cloud Stream]

== Dependencies

include::partial$dependencies.adoc[]

== Running the Application

include::partial$running-application.adoc[]

By default, when running the application in dev mode, the Dev Service is shared across multiple applications running simultaneously: the application connects to a running RabbitMQ Dev Service started by another application if available, or else starts its own that other applications can discover in turn. The applications sharing the Dev Service must use the same credentials.

== Accessing the Management Console

The application logs will show you the URL where you can access the RabbitMQ Management Console.

[source,logs]
----
...i.a.d.s.rabbitmq.ArconiaRabbitMqContainer : RabbitMQ Management Console: http://localhost:<port>
----

By default, you can log in with username `arconia` and password `arconia`.

== Configuring the Dev Service

You can configure the Dev Service via configuration properties.

|===
|Property |Default |Description

| `arconia.dev.services.{service-name}.enabled`
| `true`
| Whether the dev service is enabled.

| `arconia.dev.services.{service-name}.image-name`
| `rabbitmq`
| Full name of the container image used in the dev service.

| `arconia.dev.services.{service-name}.environment`
| `{}`
| Environment variables to set in the service.

| `arconia.dev.services.{service-name}.network-aliases`
| `[]`
| Network aliases to assign to the dev service container.

| `arconia.dev.services.{service-name}.port`
| `0`
| Fixed port for exposing the RabbitMQ AMQP port to the host. When it's 0 (default), a random available port is assigned dynamically.

| `arconia.dev.services.{service-name}.resources`
| `[]`
| Resources from the classpath or host filesystem to copy into the container. They can be files or directories that will be copied to the specified destination path inside the container at startup and are immutable (read-only).

| `arconia.dev.services.{service-name}.reuse`
| `false`
| Whether the container used in the dev service is reused across multiple applications and application restarts, relying on the Testcontainers reusable containers feature. Only applicable in dev mode.

| `arconia.dev.services.{service-name}.shared`
| `true`
| Whether the dev service is shared among applications running simultaneously. A shared dev service is discoverable by other applications, and the application connects to an existing shared dev service if available instead of starting a new one. Only applicable in dev mode.

| `arconia.dev.services.{service-name}.startup-timeout`
| `30s`
| Maximum waiting time for the service to start.

| `arconia.dev.services.{service-name}.volumes`
| `[]`
| Files or directories to mount from the host filesystem into the container. They are mounted at the specified destination path inside the container at startup and are mutable (read-write). Changes in either the host or the container will be immediately reflected in the other.

| `arconia.dev.services.{service-name}.management-console-port`
| `0`
| Fixed port for exposing the RabbitMQ Management Console to the host. When it's 0 (default), a random available port is assigned dynamically.

| `arconia.dev.services.{service-name}.username`
| `arconia`
| Username for the RabbitMQ administrator user.

| `arconia.dev.services.{service-name}.password`
| `arconia`
| Password for the RabbitMQ administrator user.
|===

include::partial$disabling-dev-service.adoc[]
